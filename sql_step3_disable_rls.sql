-- ============================================================
-- SQL 文件 3 / 3：关掉 admin_logs 的行级安全（RLS）
-- 操作：复制下面这一行 → Supabase SQL Editor → 新窗口粘贴 → Run
-- ============================================================

ALTER TABLE admin_logs DISABLE ROW LEVEL SECURITY;

-- 为什么要执行？因为项目不用 Supabase 自带登录（用自定义 JWT），
-- RLS 开启后反而会因为"类型不匹配"报错；我们后端 API 已经有权限校验，
-- 关掉 RLS 更简洁，安全不受影响。
