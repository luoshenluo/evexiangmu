# 花园全栈网页小游戏 — 项目说明书

## 一、项目概述

- **项目名**：garden-game v1.0.0
- **类型**：花园题材的全栈网页模拟经营小游戏（单仓库、前后端一体）
- **主要玩法**：种植养花、交易市场、好友互动、家族社交、小游戏、任务成就、管理员后台

## 二、技术栈

| 层级 | 技术 |
|---|---|
| 前端框架 | Next.js 14.2.33（App Router，`app/` 目录），React 18.3.1 |
| 语言 | TypeScript 5.5.4（`strict: true`，路径别名 `@/*`） |
| 样式 | Tailwind CSS 3.4.7 + tailwind-merge + clsx，自定义 `garden/flower/rank` 主题色板、季节动画 |
| 状态管理 | Zustand 4.5.4（客户端全局状态，localStorage 持久化） |
| 图标 | lucide-react |
| 后端 | Next.js Route Handlers（`app/api/*`），标注 `runtime = 'edge'` 兼容 Cloudflare Pages |
| 认证 | jose（JWT HS256，7 天过期）+ bcryptjs 密码哈希，Bearer Token，`SUPER_ADMIN_ID` 超级管理员 |
| 数据库 | Supabase（PostgreSQL），服务端仅用 `service_role` key 直连（绕过 RLS），另有本地兜底 JSON（`.game-data/db.json`） |
| 数据库访问 | @supabase/supabase-js + `pg`（本地初始化脚本 scripts/init-db.js） |
| 部署 | Cloudflare Pages（@cloudflare/next-on-pages + wrangler 4），产物 `dist/` / `.vercel/output/static` |
| 工程化 | ESLint 8 + eslint-config-next、PostCSS 8、`.npmrc` 开启 `legacy-peer-deps` |

## 三、架构要点

- 单仓库一体化：页面（`app/*`）+ 客户端组件（`components/`）+ 服务端数据层（`lib/server-store.ts`，约 3100 行，DB 行 ↔ User 对象映射）+ 路由处理器（`app/api/`）
- 前端只持有 `anon/publishable` key；服务端持有 `service_role` 密钥完成全部数据读写，RLS 关闭/绕过
- 多套环境变量命名兼容（`SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_URL` / `VITE_SUPABASE_URL` 等），支持多部署环境
- 核心配置前后端共享于 `lib/game-data.ts`（花/种子/工具、季节周期、家族等级、偷花/虫灾配置、轮盘奖励、敏感词）

## 四、功能模块

| 模块 | 说明 | 对应 API |
|---|---|---|
| 账号 | 注册/登录、签到（连续奖励）、游客/离线模式、在线心跳、个人资料与外观（主题/花园背景） | `api/auth`、`api/user/*` |
| 花园 | 地块解锁/种植/浇水/施肥/除虫/加速/收获、离线结算、季节系统（8 小时一季，春夏秋冬循环）、8 种花 + 7 级品质（黑铁→传说）、花园守卫保护、互动点赞/帮浇水 | `api/garden/*`、`api/game/state` |
| 虫灾 | 单株虫害 + 全园虫灾事件（轻微/严重/灾难，未处理死亡） | `api/garden/pest-check` |
| 偷花 | 每日限次、好友/陌生人成功率差异、同地块冷却、受害者补偿 | `api/garden/steal*` |
| 市场 | 挂售/收购/购买/出售、官方与玩家列表、花束交易 | `api/market/*` |
| 背包 | 扩容、丢弃、出售、道具（水壶/化肥/除虫剂/加速卡/花园守卫） | `api/inventory/*` |
| 任务 | 每日/每周/每月任务，管理员可配置模板，含家族集体任务 | `api/tasks` |
| 成就与称号 | `achievements` / `titles`，签到龙等称号奖励 | — |
| 社交 | 好友申请/好友列表/私聊、家族（创建/加入/退出、升级至 10 级 100 人上限、角色权限、贡献） | `api/friends`、`api/family/*`、`api/chat/private` |
| 聊天 | 世界/家族/好友频道 + 私聊，敏感词过滤、频率限制、禁言 | `api/chat/*` |
| 小游戏 | 幸运转盘（花瓣代币）、猜大小（dice） | `api/user/wheel`、`api/minigame/dice` |
| 工坊 | 制作花束、培育珍稀品种 | `api/workshop/*` |
| CDK | 兑换码生成与兑换 | `api/cdk/redeem`、`api/admin/cdks` |
| 通知 | 系统/交易/好友/家族等多类型通知 | `api/notifications` |
| 排行榜 | 用户排行 | `api/rankings` |
| 管理后台 | 用户管理/禁言封号、公告、聊天与敏感词、CDK、市场调控、权限管理（8 位 bitwise 权限位）、操作日志、统计、经济调控、任务管理、修改密码 | `api/admin/*` |

## 五、数据模型（supabase/schema.sql）

核心表：`users`、`families`、`messages`、`listings`、`buy_orders`、`user_tasks`、`cdks`、`notifications`、`game_state`、`announcements`、`steal_logs`、`plot_steal_records`。

另有迁移 SQL（`sql_step*.sql`、`supabase-*.sql`、`migration_*.sql`）覆盖热更新、管理日志、禁用 RLS、私聊等迭代；多数复杂字段以 JSONB 存储（`plots`、`inventory`、`achievements` 等）。

## 六、关键配置文件

- `next.config.js`：reactStrictMode、远程图片白名单、next-on-pages 兼容
- `wrangler.toml`：Cloudflare Pages 构建产物指向 `.vercel/output/static`，`nodejs_compat`
- `.env.example`：`NEXT_PUBLIC_SUPABASE_URL`、`NEXT_PUBLIC_SUPABASE_ANON_KEY`、`SUPABASE_SERVICE_ROLE_KEY`、`JWT_SECRET`